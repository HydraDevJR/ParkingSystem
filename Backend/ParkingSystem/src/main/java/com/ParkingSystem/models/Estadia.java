package com.ParkingSystem.models;

public class Estadia {
    
    public enum EstadoEstadia {
        En_Curso,
        Finalizada,
        Cancelada
    }
    
    //id, vehiculo(Vehiculo), celda(Celda), fecha inicio, fecha fin, fecha creacion, fecha modificacion, estado(activa, finalizada, anulada), reserva(Reserva))
}

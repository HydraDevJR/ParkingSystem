package com.ParkingSystem.models;

public class Ticket {
    
    public enum EstadoTicket {
        Abierto,
        Cerrado,
        Anulado
    }

    //id, estadia, fecha de creacion, estado(abierto, cerrado, anulado)
}

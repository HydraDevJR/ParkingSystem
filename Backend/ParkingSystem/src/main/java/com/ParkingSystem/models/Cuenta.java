package com.ParkingSystem.models;

public class Cuenta {

    public enum TipoCuenta {
    //CuentaAdmin, CuentaUsuario
    CuentaAdmin,
    CuentaUsuario
    }
    
    //id, usuario, username, contraseña, ultimo acceso, fecha creacion, fecha modificacion, activo, tipo(cuenta admin, cuenta usuario)
}
